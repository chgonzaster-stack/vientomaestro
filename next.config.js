/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["example.com"], // reemplaza con los dominios que realmente usarás
  },
};

module.exports = nextConfig;
